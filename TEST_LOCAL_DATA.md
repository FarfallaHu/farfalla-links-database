# 本地数据系统 - 使用说明

## ✅ 完成的改造

你的应用现在已经完全本地化了！不再需要 Supabase。

### 工作原理：

1. **静态数据**（193个链接 + 21个文件夹）
   - 存储在：`src/app/data/localData.ts`
   - 所有访问网站的人都能看到这些数据

2. **个人数据**（你添加/编辑的内容）
   - 存储在：浏览器的 localStorage
   - 只有你能看到你的修改
   - 包括：新添加的链接、编辑的标题、删除的项目等

### 数据合并规则：

**别人看到：**
- 你的 193 个静态链接和 21 个文件夹（只读）

**你看到：**
- 静态数据 + 你的个人修改
- 例如：静态193个链接 - 你删除的5个 + 你新添加的10个 = 198个链接

## 🎯 功能状态

### ✅ 保留的功能（完全可用）：
- ✅ 查看所有链接和文件夹
- ✅ 添加新链接
- ✅ 删除链接/文件夹
- ✅ 编辑链接标题
- ✅ 重命名文件夹
- ✅ 创建新文件夹
- ✅ 更改文件夹图标（仅 emoji）
- ✅ 拖拽排序
- ✅ 移动链接到不同文件夹
- ✅ 密码保护（编辑/删除时需要输入密码）

### ❌ 移除的功能（需要后端）：
- ❌ 自动获取网页标题（metadata）
- ❌ 上传自定义文件夹图标（图片文件）
- ❌ 现在只能使用 emoji 作为文件夹图标

## 📝 使用注意事项

### 添加链接时：
- URL 必填
- Title 选填（如果不填，会使用 URL 作为标题）
- Favicon 会自动设置为 `网站域名/favicon.ico`

### 数据持久化：
- 你的个人数据存在浏览器的 localStorage 中
- **清除浏览器数据会丢失你的个人修改**
- 静态数据不会丢失（在代码文件中）

### 重置数据：
如果你想重置到原始静态数据，可以在浏览器控制台运行：
```javascript
import('./utils/dataManager').then(m => m.resetToStaticData())
```

## 🔄 更新静态数据

如果你想更新静态数据文件（从当前状态导出），可以：

1. 打开浏览器控制台
2. 运行以下代码查看当前所有数据：
```javascript
import('./utils/dataManager').then(m => {
  console.log('Links:', m.getAllLinks())
  console.log('Folders:', m.getAllFolders())
})
```

3. 手动复制粘贴到 `src/app/data/localData.ts` 文件中

## 🚀 部署

把生产构建的 `dist` 内容部署到 Hostinger，所有访问者都能看到你的链接收藏！详细步骤见 [HOSTINGER_DEPLOYMENT.md](./HOSTINGER_DEPLOYMENT.md)。

## ⚠️ 重要提醒

- 静态数据对所有人可见（只读）
- 你的个人修改只存在你的浏览器中
- 其他用户看不到你的个人修改
- 如果你想让其他人看到你的新链接，需要将它们添加到 `localData.ts` 文件中

---

**享受你的全新本地化链接数据库！** 🎉
